import { Box, Typography } from "@mui/material";

const ApelHeader = () => {
  return (
    <Box sx={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
      <Typography fontSize="22px">Apel</Typography>
    </Box>
  );
};

export default ApelHeader;
